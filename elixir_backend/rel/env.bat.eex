@echo off
REM Sets up the environment for the release

set RELEASE_NAME=<%= @release.name %>
set RELEASE_VSN=<%= @release.version %>
set RELEASE_COOKIE=links_api_cookie
set RELEASE_NODE=<%= @release.name %>@127.0.0.1
